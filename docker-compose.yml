version: '3.8'

services:
  shuttle-app:
    build: .
    ports:
      - "3001:3001"
    volumes:
      - shuttle-data:/app/data
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_STORAGE=/app/data/shuttle.db
    restart: unless-stopped

volumes:
  shuttle-data:
